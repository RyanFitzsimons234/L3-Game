// Define the height of the stage floor
export const STAGE_FLOOR = 216;

// Define the width and height of the stage
export const STAGE_WIDTH = 768;
export const STAGE_HEIGHT = 256;

// Calculate the midpoint of the stage width
export const STAGE_MID_POINT = STAGE_WIDTH / 2;

// Define the amount of padding around the stage
export const STAGE_PADDING = 256;

// Define a scroll boundary for camera scrolling
export const SCROLL_BOUNDARY = 100;
